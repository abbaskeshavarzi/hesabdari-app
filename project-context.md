# زمینه پروژه: سامانه حسابداری «نوبر» (hesabdari-app)

این سند خلاصه کامل و به‌روزشده‌ی پروژه است تا در چت‌های جدید بدون نیاز به توضیح مجدد، ادامه کار ممکن باشه.
(نسخه‌ی قبلی این فایل رو جایگزین کن.)

## معرفی کلی
- **نام کسب‌وکار:** نوبر
- **نام ریپازیتوری گیت‌هاب:** `hesabdari-app` (صاحب: abbaskeshavarzi)
- **آدرس زنده اپ:** `https://abbaskeshavarzi.github.io/hesabdari-app/`
- **هدف:** وب‌اپ حسابداری شخصی برای مدیریت مشتریان، کالا، انبار، فاکتور، هزینه و گزارش‌های مالی — کاملاً رایگان، بدون سرور اختصاصی.
- **سطح فنی کاربر:** مبتدی (بدون دانش برنامه‌نویسی) — همه تغییرات باید با دستورات آماده و توضیح قدم‌به‌قدم انجام بشه.
- **محیط کار کاربر:** فقط گوشی موبایل (اندروید، کروم)، از طریق GitHub Codespaces در مرورگر.

## پشته فنی (Tech Stack)
- **فرانت‌اند:** Next.js 14 (Pages Router)، خروجی استاتیک (`output: 'export'`)
- **استایل:** Tailwind CSS، فونت Vazirmatn، طراحی راست‌به‌چپ (RTL)
- **پایگاه‌داده و احراز هویت:** Supabase (PostgreSQL + Supabase Auth + Supabase Storage)
- **میزبانی:** GitHub Pages (رایگان)
- **CI/CD:** GitHub Actions — با هر `push` به شاخه `main`، به‌صورت خودکار build و منتشر می‌شه (فایل `.github/workflows/deploy.yml`)
- **basePath:** چون آدرس زیر `/hesabdari-app/` منتشر می‌شه، مسیرهای مطلق باید با `/hesabdari-app/` هماهنگ باشن. این مقدار از طریق `process.env.NEXT_PUBLIC_BASE_PATH` (تعریف‌شده در `next.config.js`) در کد سمت کلاینت استفاده می‌شه، نه هاردکد.

## گردش‌کار استاندارد برای هر تغییر
1. فایل zip جدید پروژه آپلود می‌شه در Codespace (جایگزین نسخه قبلی)
2. در ترمینال:
   ```
   unzip -o accounting-app.zip
   shopt -s dotglob && cp -rf accounting-app/* . && rm -rf accounting-app accounting-app.zip
   git add .
   git commit -m "<توضیح تغییر>"
   git push
   ```
3. اگه پکیج جدیدی اضافه شده باشه (مثل قبلاً `chart.js`، `react-chartjs-2`)، قبل از commit یک بار `npm install` هم اجرا می‌شه.
4. اگه تغییر دیتابیس لازم باشه، کاربر SQL داده‌شده رو در **Supabase Dashboard → SQL Editor** اجرا می‌کنه (ترجیحاً قبل از push).
5. انتشار خودکار حدود ۱-۲ دقیقه طول می‌کشه؛ می‌شه در تب **Actions** ریپازیتوری وضعیتش رو چک کرد. بعد از انتشار، کاربر باید تب مرورگر رو کامل ببنده و دوباره باز کنه (کش سرویس‌ورکر ممکنه نسخه‌ی قدیمی رو نگه داره).
6. **[جدید] قبل از push، از چک‌لیست استقرار پیروی کن** (بررسی کد → SQL در صورت نیاز → آپلود/ترمینال → چک تب Actions → تست دستی روی سایت زنده).

## ساختار پایگاه‌داده (جداول موجود)
همه جداول دارای Row Level Security (RLS) هستن؛ فقط کاربر لاگین‌کرده (`authenticated`) دسترسی داره. **[نکته مهم: این مدل تک‌کاربره/تک‌سازمانیه — همه‌ی کاربران لاگین‌کرده به همه‌ی داده دسترسی دارن، بدون تفکیک سازمان. برای گسترش، بخش «مسائل شناخته‌شده» رو ببین.]**

| جدول | توضیح | فیلدهای کلیدی |
|---|---|---|
| `customers` | مشتریان | name, phone, address |
| `products` | کاتالوگ کالا | name, unit, price, stock_qty |
| `stock_movements` | تاریخچه ورود/خروج انبار | product_id, change_qty, reason |
| `invoices` | سرفصل فاکتور | customer_id, invoice_number, issue_date, total_amount, status, description, discount_type, discount_value, discount_amount |
| `invoice_items` | اقلام فاکتور | invoice_id, product_id, product_name, quantity, unit_price |
| `payments` | پرداخت‌های مشتری | customer_id, amount, payment_date, note |
| `business_settings` | اطلاعات کسب‌وکار (تک‌ردیفه، id='default') | name, phone, address, logo_url |
| `expenses` | هزینه‌ها | category, amount, expense_date, description |

**View محاسباتی:** `customer_balances` — مانده حساب هر مشتری (مجموع فاکتور منهای مجموع پرداخت) را خودکار محاسبه می‌کند.

**ایندکس‌ها:** `idx_invoices_customer_id`, `idx_invoices_issue_date` — روی جدول `invoices`، برای سریع‌تر شدن فیلتر/مرتب‌سازی با رشد داده.

**Storage bucket:** `logos` — برای نگهداری لوگوی کسب‌وکار (عمومی برای خواندن، محدود به کاربر لاگین‌کرده برای نوشتن).

### توابع دیتابیسی (RPC) — مهم
سه عملیات به‌صورت **اتمیک** (تراکنشی) سمت دیتابیس انجام می‌شن، نه چندمرحله‌ای سمت فرانت‌اند:

- **`create_invoice_with_items(p_customer_id, p_invoice_number, p_issue_date, p_description, p_status, p_items jsonb, p_discount_type default 'amount', p_discount_value default 0)`**
  ثبت فاکتور + اقلام + کسر موجودی + محاسبه‌ی تخفیف، همه با هم. اگه موجودی کافی نباشه، با پیام فارسی خطا می‌ده.
- **`delete_invoice_and_restore_stock(p_invoice_id)`**
  حذف فاکتور + بازگرداندن موجودی کالاهای اون فاکتور به انبار + ثبت در تاریخچه‌ی انبار.
- **`get_dashboard_stats()`**
  همه‌ی آمار داشبورد (تعداد مشتری، مطالبات باز، فروش ماه، نمودار ۶ ماهه، بدهکارترین مشتریان، فاکتورهای معوق قدیمی، آخرین فاکتورها) رو در یک درخواست و به‌صورت تجمیع‌شده سمت SQL برمی‌گردونه — به‌جای اینکه کل جدول `invoices`/`customer_balances` به مرورگر فرستاده بشه و محاسبات سمت جاوااسکریپت انجام بشه. `pages/index.js` الان فقط همین یک RPC رو صدا می‌زنه. **[جدید] خروجی `overdue_invoices` و `recent_invoices` الان `id` فاکتور رو هم داره (و `recent_invoices` نام مشتری رو هم داره)، چون داشبورد الان ردیف‌ها رو مستقیم به `/invoice-print?id=...` لینک می‌کنه.**

فایل‌های migration به ترتیب اجرا شدن (همه در ریشه‌ی پروژه موجودن، شماره‌گذاری‌شده):
`supabase-schema.sql` (پایه) → `migration-2-inventory` → `migration-3-invoice-items` → `migration-4-invoice-status` → `migration-5-business-settings` → `migration-6-expenses` → `migration-7-invoice-rpc` → `migration-8-delete-invoice-rpc` → `migration-9-invoice-discount` → `migration-10-dashboard-stats` (تابع `get_dashboard_stats` + دو ایندکس بالا) → **[جدید] `migration-11-dashboard-links`** (بازنویسی `get_dashboard_stats` فقط برای افزودن `id`/نام مشتری به دو تا از لیست‌ها؛ امضای تابع عوض نشده، نیازی به `drop function` نبود).

⚠️ اگه در آینده امضای یه تابع RPC عوض بشه، حتماً باید `drop function if exists ...(امضای دقیق قبلی)` قبل از `create or replace function` بیاد، وگرنه Supabase دو تابع هم‌نام نگه می‌داره و خطای ابهام می‌ده.

**[جدید] راهنمای ساخت RPC جدید:** فایل `docs/rpc-guide.md` — چک‌لیست کامل (الگوی `security definer`+`search_path`، `revoke/grant`، پیام خطای فارسی، قفل ردیف، `drop function if exists` موقع تغییر امضا) بر اساس همین سه RPC بالا نوشته شده. هر بار RPC جدید لازم شد، اول همین فایل رو ببین.

## ماژول‌ها و صفحات پیاده‌شده
- `/` داشبورد — آمار از طریق RPC تجمیعی `get_dashboard_stats` (نه گرفتن کل جدول)، نمودار فروش ۶ ماهه با **Chart.js تعاملی** (tooltip فارسی، به‌جای میله‌های CSS خام قبلی)، بدهکارترین مشتریان، فاکتورهای معوق قدیمی‌تر از ۳۰ روز، Skeleton loading. مدیریت خطا: از `friendlyError` استفاده می‌کنه، `stats` در صورت خطا به یه حالت خالی (نه `null`) ست می‌شه (جلوگیری از Skeleton بی‌پایان)، و گارد `!data` برای جلوگیری از کرش اضافه شده. **[جدید، بعد از بازبینی UX] چهار بهبود:**
  - دکمه‌ی «افزودن سریع» بالای داشبورد (دکمه‌ی برجسته‌ی «+ فاکتور جدید» + فلش کوچیک برای «+ مشتری»/«+ هزینه»)؛ با `?new=1` صفحه‌ی مقصد فرم افزودن رو خودکار باز می‌کنه.
  - ردیف‌های «بدهکارترین مشتریان»، «فاکتورهای معوق» و «آخرین فاکتورها» قابل کلیک شدن و مستقیم به مشتری (`/customers?search=...`) یا فاکتور (`/invoice-print?id=...`) لینک می‌شن؛ «آخرین فاکتورها» الان نام مشتری و شماره فاکتور هم نشون می‌ده (قبلاً فقط تاریخ/مبلغ بود).
  - نشانگر روند فروش این ماه نسبت به ماه قبل (▲/▼ + درصد) کنار کارت «فروش این ماه».
  - در حالت خطا، کارت‌های آماری «—» نشون می‌دن (نه «۰») تا با صفر واقعی اشتباه نشن.
- `/customers` مشتریان (CRUD، جست‌وجو، خروجی CSV، وضعیت حساب، صفحه‌بندی، Skeleton loading). **[جدید]** پشتیبانی از `?new=1` (باز کردن خودکار فرم افزودن) و `?search=...` (پرشدن خودکار جست‌وجو) برای لینک‌های داشبورد.
- `/products` کالاها (CRUD، قیمت، موجودی، صفحه‌بندی، Skeleton loading)
- `/inventory` انبار (ثبت ورود/خروج، تاریخچه، Skeleton loading)
- `/invoices` فاکتورها (چند قلم کالا، شماره‌گذاری خودکار، تخفیف مبلغ‌ثابت/درصدی، وضعیت پرداخت، ثبت/حذف اتمیک از طریق RPC، جست‌وجو، CSV، صفحه‌بندی، Skeleton loading). **[جدید]** پشتیبانی از `?new=1` و `?search=...` برای لینک‌های داشبورد.
- `/invoice-print` چاپ فاکتور (چاپ مرورگر، دانلود PDF واقعی، ذخیره عکس، پیامک، واتساپ، نمایش تخفیف) — مستقل از منوی اصلی
- `/payments` پرداخت‌ها (صفحه‌بندی، Skeleton loading)
- `/expenses` هزینه‌ها (دسته‌بندی‌شده، صفحه‌بندی، Skeleton loading)
- `/reports` گزارش فروش دوره‌ای (Skeleton loading)
- `/profit-loss` گزارش سود و زیان
- `/best-performers` پرفروش‌ترین کالاها و بهترین مشتریان
- `/backup` پشتیبان‌گیری کامل — خروجی گرفتن از تمام جداول به یک فایل JSON، و بازیابی از همون فایل
- `/settings` تنظیمات کسب‌وکار (نام، تلفن، آدرس، لوگو)
- `/login` ورود

## کامپوننت‌ها و ابزارهای مشترک
- `components/Layout.js` — هدر/منو با معماری واکنش‌گرا: دسکتاپ سایدبار ثابت، موبایل نوار بالای فشرده با دکمه‌ی همبرگری. شامل جست‌وجوی سراسری، حالت تاریک/روشن، خروج از حساب، نوار هشدار آفلاین بودن.
- `components/GlobalSearch.js` — پنجره‌ی جست‌وجوی سراسری (مشتریان + کالاها + شماره فاکتور هم‌زمان)
- `components/Pagination.js` — کنترل صفحه‌بندی مشترک (۱۵ ردیف در هر صفحه، سمت کلاینت روی داده‌ی فیلترشده)
- `components/Skeleton.js` — کامپوننت‌های اسکلت بارگذاری (`SkeletonBar`, `TableSkeleton`, `CardSkeleton`, `ListSkeleton`) که جایگزین متن ساده‌ی «در حال بارگذاری…» شدن؛ در تمام صفحات جدولی و داشبورد استفاده می‌شن.
- `components/MoneyInput.js` — ورودی مبلغ با جداکننده سه‌رقمی خودکار
- `components/JalaliDatePicker.js` — تقویم شمسی سفارشی
- `lib/jalaliCalendar.js` — توابع تبدیل میلادی↔شمسی
- `lib/dateFormat.js` — نمایش تاریخ شمسی در جدول‌ها
- `lib/csv.js` — خروجی گرفتن CSV با پشتیبانی از فارسی (BOM UTF-8)
- `lib/errorMessages.js` — تابع `friendlyError(error, fallback)` برای تبدیل خطاهای خام Postgres/Supabase به پیام فارسی قابل‌فهم. همه‌ی صفحات (از جمله داشبورد) از این تابع استفاده می‌کنن. **[جدید] لحن یکدست شد:** همه‌ی پیام‌ها (هم `CODE_MESSAGES` داخل خود فایل، هم fallback هایی که هر صفحه بهش می‌ده) طبق الگوی «چه اتفاقی افتاد + کاربر چیکار کنه» بازنویسی شدن (مثلاً «خطا در ثبت مشتری.» شد «خطا در ثبت مشتری. لطفاً دوباره تلاش کنید.»).
- `lib/supabaseClient.js` — اتصال به Supabase

## اسناد مرجع پروژه [جدید]
دو فایل مرجع جدید در پوشه‌ی `docs/` اضافه شدن (کنار همین فایل نگه‌داری بشن و هر وقت لازم شد در چت جدید آپلود بشن):
- **`docs/rpc-guide.md`** — چک‌لیست ساخت تابع RPC جدید، بر اساس الگوهای همین پروژه.
- **`docs/supabase-runbook.md`** — رفع ۷ مشکل رایج (خطای session/لاگین، RLS، تابع دوپهلو، کش سرویس‌ورکر، basePath، pause شدن پروژه‌ی رایگان Supabase، خطای اجرای migration).

## قابلیت آفلاین (PWA)
`public/sw.js` — سرویس‌ورکر «stale-while-revalidate». صفحات/فایل‌های استاتیک قبلاً بازدیدشده حتی بدون اینترنت نمایش داده می‌شن. درخواست‌های Supabase هرگز کش نمی‌شن.
**محدودیت مهم:** فقط برای *نمایش*؛ ثبت/ویرایش داده همچنان نیاز به اینترنت داره.

## طراحی و تم
- رنگ‌ها متغیر CSS: `ink`(#... متغیر)، `paper`، `surface`، `line`(#C7CCCB روشن / #263042 تاریک)، `brass`(#B8873B)/`brassDark`(#96692A)، `good`(#2F6E4F)/`bad`(#A33B34)
- نوار کناری/هدر همیشه رنگ ثابت تیره (`#12182B`) دارد، مستقل از حالت روشن/تاریک
- فونت Vazirmatn از CDN
- متا تگ `viewport` شامل `initial-scale=1`
- **[جدید، بعد از بررسی کنتراست WCAG] دو توکن رنگی جدید: `badText`/`goodText`.** `good`/`bad` اصلی فقط برای پس‌زمینه‌ی توپر (دکمه‌ها، با متن سفید روش) استفاده می‌شن و ثابت موندن. `badText`/`goodText` (متغیر CSS، مثل `ink`) برای **متن** قرمز/سبز روی سطح استفاده می‌شن؛ در حالت تاریک روشن‌تر از نسخه‌ی روشن‌ان (`#E0655A`/`#4FA377` به‌جای `#A33B34`/`#2F6E4F`) چون نسخه‌ی اصلی روی پس‌زمینه‌ی تیره‌ی کارت به کنتراست AA (۴.۵:۱) نمی‌رسید (۲.۷:۱ بود). همه‌ی جاهای `text-bad`/`text-good` در کل پروژه به `text-badText`/`text-goodText` تغییر کردن. آیتم فعال منو هم اصلاح شد (رنگ متن ثابت `#17233D` به‌جای `ink` که در تاریک روشن می‌شد و روی brass کنتراستش می‌افتاد).

## متغیرهای محیطی لازم (Secrets در GitHub + `.env.local` در Codespace)
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## کتابخانه‌های کلیدی سمت کلاینت
- `modern-screenshot` — رندر DOM به تصویر (برای «ذخیره به‌صورت عکس»)
- `jspdf` — ساخت فایل PDF واقعی از فاکتور (تصویر رندرشده در یک صفحه‌ی PDF هم‌اندازه)
- `chart.js` + `react-chartjs-2` — نمودار ستونی تعاملی فروش ۶ ماهه در داشبورد (جایگزین CSS خام قبلی)

## مسائل حل‌شده در این چرخه از کار (به‌روز)
- بریدگی تصویر فاکتور در آیفون → حل با `modern-screenshot`
- بهم‌ریختگی چیدمان/مقیاس صفحه در گوشی → حل با اصلاح `viewport` و منوی موبایل
- کسر ناهماهنگ موجودی هنگام ثبت هم‌زمان چند فاکتور → حل با RPC اتمیک + قفل ردیف
- عدم بازگشت موجودی هنگام حذف فاکتور → حل با RPC اختصاصی
- کندی بالقوه‌ی داشبورد با رشد داده → حل با RPC تجمیعی `get_dashboard_stats` + ایندکس روی `invoices`
- نمودار غیرتعاملی داشبورد → حل با Chart.js (tooltip فارسی)
- نبود بازخورد بصری هنگام بارگذاری → حل با Skeleton loading در همه‌ی صفحات جدولی و داشبورد
- مدیریت خطای ناقص داشبورد (پیام ثابت غیر یکسان با بقیه‌ی پروژه + Skeleton بی‌پایان در صورت خطا + احتمال کرش با `data` خالی) → حل با `friendlyError` + مقداردهی خالی به `stats` در خطا + گارد `!data`.
- **[جدید] نبود اقدام سریع در داشبورد** (کاربر باید از منو می‌رفت صفحه‌ی دیگه) → حل با دکمه‌ی «افزودن سریع» (فاکتور/مشتری/هزینه با `?new=1`).
- **[جدید] ردیف‌های غیرقابل‌کلیک در داشبورد** (بدهکاران، فاکتورهای معوق، آخرین فاکتورها) → حل با لینک مستقیم هر ردیف به مشتری/فاکتور مربوطه؛ «آخرین فاکتورها» نام مشتری و شماره فاکتور هم گرفت.
- **[جدید] کنتراست ناکافی رنگ‌های قرمز/سبز متنی و متن آیتم فعال منو در تم تاریک** (زیر آستانه‌ی WCAG AA) → حل با توکن‌های جدید `badText`/`goodText` (روشن‌تر در تاریک) + رنگ ثابت برای متن آیتم فعال منو.
- **[جدید] لحن ناهماهنگ پیام‌های خطا** (بعضی راهنمایی عملی داشتن، بعضی نه) → حل با یکدست‌سازی همه‌ی fallback ها و `CODE_MESSAGES` طبق الگوی «چه اتفاقی افتاد + چیکار کنه».

## مسائل شناخته‌شده (هنوز حل نشده / پیشنهاد برای آینده)
- **نبود تست خودکار** و عدم امکان build واقعی در sandbox — تغییرات با بازبینی دستی کد (esbuild برای چک syntax) + تست دستی کاربر روی سایت زنده تأیید می‌شن.
- **صفحه‌بندی سمت کلاینت** (نه سمت سرور/دیتابیس) — تصمیم آگاهانه برای سادگی جست‌وجو؛ با رشد خیلی زیاد داده (چند هزار ردیف) نیاز به بازطراحی داره.
- **پیش‌فاکتور** پیاده نشده.
- **حالت تاریک خودکار هماهنگ با گوشی** (`prefers-color-scheme`) پیاده نشده؛ فعلاً فقط دستیه.
- **گزارش خطای خودکار از اپ** (مثل Sentry) وجود نداره.
- **دکمه‌ی افزودن سریع** در داشبورد پیاده نشده.
- **چندمستأجری‌سازی — تصمیم‌گیری در جریان، هنوز شروع نشده.** دو مسیر بررسی و برای کاربر توضیح داده شد؛ کاربر هنوز تصمیم نگرفته کدوم رو می‌خواد:
  - **مسیر ۱ (چند کاربر روی یک حساب):** افزودن جدول `user_roles` + تغییر RLS برای تفکیک نقش (مدیر/کارمند). ریسک پایین-متوسط، ساختار داده تغییر نمی‌کنه.
  - **مسیر ۲ (SaaS واقعی برای فروش به چند کسب‌وکار):** افزودن `organization_id` به هر ۸ جدول، بازنویسی کامل RLS و هر دو RPC فاکتور (و `get_dashboard_stats`، که چون `security definer` است باید فیلتر سازمان بگیره)، جدول‌های `organizations`/`organization_members`، جریان ثبت‌نام سازمان جدید، migration داده‌ی موجود، بازبینی همه‌ی کوئری‌ها. ریسک بالا (نشت داده بین مشتری‌ها در صورت خطای RLS)؛ **توصیه شد قبل از این مسیر امکان اجرای واقعی build/تست (نه فقط بازبینی دستی) فراهم بشه**، و مسائل غیرفنی (قیمت‌گذاری، billing) هم برنامه‌ریزی بشه. این باید به‌عنوان یه پروژه‌ی جدا/بزرگ دیده بشه، نه یه قدم کوچیک مثل کارهای این چرخه.

## ترجیحات کاربر در روند کار
- توضیحات همیشه باید فارسی، ساده، و قدم‌به‌قدم باشه (کاربر مبتدیه).
- بعد از هر تغییر کد، همیشه فایل zip جدید پروژه ارائه بشه + دستورات دقیق ترمینال (بدون فرض دانش قبلی).
- اگه تغییر دیتابیس لازمه، SQL دقیق و آماده کپی‌پیست ارائه بشه — **جدا جدا و به‌صورت متن آماده کپی** (نه فقط داخل zip).
- قبل از شروع کارهای بزرگ جدید، با یک سؤال کوتاه (چندگزینه‌ای) اولویت‌بندی/دامنه از کاربر پرسیده بشه (مثلاً قبل از چندمستأجری‌سازی).
- این محیط توسعه (sandbox) به اینترنت/npm registry دسترسی نداره، پس `npm install`/build واقعی اینجا قابل تست نیست؛ کدها با بازبینی دستی + ابزار esbuild برای چک syntax (نه اجرای واقعی build) تأیید می‌شن. این نکته باید در چت جدید هم در نظر گرفته بشه.
- **[جدید] قبل از push، طبق چک‌لیست استقرار پیش برو** (بررسی کد → SQL در صورت نیاز → آپلود/ترمینال → چک Actions → تست دستی سایت زنده).
